<!doctype html>
<html lang="vi"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>{{ ($register??false)?'Đăng ký':'Đăng nhập' }} | MOFI</title>@vite(['resources/css/app.css','resources/js/app.js'])</head><body class="auth-page"><main class="auth-card"><a class="brand" href="/"><span class="brand-mark">M</span><span>MOFI<small>Hiểu tiền, đầu tư tốt hơn.</small></span></a><h1>{{ ($register??false)?'Bắt đầu hành trình của bạn':'Chào mừng trở lại' }}</h1><p class="auth-lead">Theo dõi tài sản, học đầu tư và thử nghiệm với tiền ảo.</p>@if($errors->any())<div class="form-error" role="alert">{{ $errors->first() }}</div>@endif<form method="post" action="{{ ($register??false)?route('register'):route('login.store') }}">@csrf @if($register??false)<label>Tên hiển thị<input type="text" name="name" value="{{ old('name') }}" required maxlength="120"></label>@endif<label>Email<input type="email" name="email" value="{{ old('email') }}" required autocomplete="email"></label><label>Mật khẩu<input type="password" name="password" required autocomplete="{{ ($register??false)?'new-password':'current-password' }}" @if($register??false) minlength="10" @endif></label>@if($register??false)<label>Nhập lại mật khẩu<input type="password" name="password_confirmation" required minlength="10" autocomplete="new-password"></label><small>Tối thiểu 10 ký tự. Tài khoản mới bắt đầu với danh mục rỗng.</small>@else<label class="check"><input type="checkbox" name="remember"> Ghi nhớ phiên đăng nhập</label>@endif<button class="button button-blue button-large" type="submit">{{ ($register??false)?'Tạo tài khoản':'Đăng nhập' }}</button></form><p class="auth-foot">{{ ($register??false)?'Đã có tài khoản?':'Bạn mới đến MOFI?' }} <a href="{{ ($register??false)?'/login':'/register' }}" class="back-link">{{ ($register??false)?'Đăng nhập':'Đăng ký miễn phí' }}</a></p><a class="back-link" href="/">← Về trang giới thiệu</a></main></body></html>
